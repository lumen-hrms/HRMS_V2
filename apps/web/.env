# Firebase web app config — project "lumen-307b1"
# (Firebase console -> Project settings -> General -> Your apps -> Web app).
# NOT secret — these ship in the built client bundle. Tracked in git so a
# fresh checkout runs; each environment can override.
#
# Only the three below are read by apps/web/src/lib/firebase.ts.
VITE_FIREBASE_API_KEY="AIzaSyAWvHZ6B-iNTBlgfbroiXvSz8P_xE09tOM"
VITE_FIREBASE_AUTH_DOMAIN="lumen-307b1.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="lumen-307b1"
