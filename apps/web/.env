# Firebase web app config — project "lumen-307b1"
# (Firebase console -> Project settings -> General -> Your apps -> Web app).
# These are NOT secrets; they ship in the built client bundle.
#
# Only the three below are read by apps/web/src/lib/firebase.ts. The rest of
# the console snippet (storageBucket, messagingSenderId, appId, measurementId)
# is only needed for Storage / Analytics, which this app doesn't use:
#   storageBucket     = lumen-307b1.firebasestorage.app
#   messagingSenderId = 343860587932
#   appId             = 1:343860587932:web:60c4fcaa1f7f030788ec59
#   measurementId     = G-H1174X05RY

VITE_FIREBASE_API_KEY="AIzaSyAWvHZ6B-iNTBlgfbroiXvSz8P_xE09tOM"
VITE_FIREBASE_AUTH_DOMAIN="lumen-307b1.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="lumen-307b1"

# Real Firebase — no emulator. To switch back to the local Auth emulator,
# uncomment the next line (and re-add FIREBASE_AUTH_EMULATOR_HOST in apps/api/.env).
# VITE_FIREBASE_AUTH_EMULATOR_HOST="localhost:9099"
