node_modules/
dist/
build/

# apps/web/.env is intentionally NOT ignored — it only holds the Firebase
# web config, which ships in the browser bundle anyway (not secret).
# The API env files DO hold secrets (Firebase service-account key, Supabase
# passwords) and stay out of git — share them via the team vault.
apps/api/.env
apps/api/.env.test
*firebase-adminsdk*.json
*-service-account*.json


*.log
.DS_Store
coverage/
.turbo/
apps/web/node_modules/
apps/api/node_modules/
.pgdata/
.pglog
